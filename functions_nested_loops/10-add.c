#include "main.h"
#include <stdio.h>
/**
*add - adds two integers
*@a: first integer
*@b: second integer
*Description: function that adds two integers and returns the result
*return : the result to add a and b
*/
int add(int a, int b)
{
	return (a + b);
}
